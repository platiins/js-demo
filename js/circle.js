const circleRadius = prompt("please enter a radius of a circle:");

const pi = 3.14;

let circleArea = circleRadius ** 2 * pi;

document.body.innerHTML = `<h1>The area of the circle is ${circleArea}</h1>`;
